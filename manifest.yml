---
# app manifest
applications:
 - name: sampleapp623
   command: bundle exec rake cf:on_first_instance db:migrate && rails s -p $PORT
   memory: 1G
   host: sampleapp623
   services:
     - elephantsql
